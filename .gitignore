node_modules/
.env
.env.local
client/dist/
client/node_modules/
*.log
.DS_Store

# Runtime state — SQLite history DB + hashed API keys file.
# Per-deployment, never in version control.
data/

# Local Claude Code config (skills, settings, etc.) — kept out of the
# public repo by deliberate choice; the project-level skill lives only
# on this dev box.
.claude/
